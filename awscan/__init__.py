"""awscan — automated web vulnerability scanner for authorized testing.

Zero runtime dependencies (Python stdlib only). Every active probe is gated
by an explicit scope file; out-of-scope targets are refused before a single
request is sent.
"""
__version__ = "1.1.0"
